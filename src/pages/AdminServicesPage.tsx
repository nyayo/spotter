import { useState } from 'react';
import { AdminShell } from '@/pages/AdminDashboardPage';

const initialServices = [{ name: 'Dinner Date', description: 'Companionship for meals and evenings', profiles: 842, active: true }, { name: 'Travel Companion', description: 'Discreet company for local and international travel', profiles: 516, active: true }, { name: 'Events & Social', description: 'Arrive together for events and social occasions', profiles: 389, active: true }, { name: 'Wellness & Spa', description: 'Relaxed wellness and self-care experiences', profiles: 206, active: false }];
export function AdminServicesPage() { const [services, setServices] = useState(initialServices); const toggle = (name: string) => setServices((items) => items.map((item) => item.name === name ? { ...item, active: !item.active } : item)); return <AdminShell activeRoute="admin-services" title="Services" badge={`${services.length} CATEGORIES`}><div className="mx-auto max-w-5xl space-y-6 p-6 lg:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-on-surface-variant">Organize the services available across the directory.</p><h1 className="mt-2 font-display text-3xl text-on-surface">Service Categories</h1></div><button onClick={() => setServices((items) => [...items, { name: 'New Service', description: 'Add a description', profiles: 0, active: false }])} className="flex items-center gap-2 self-start rounded-lg bg-primary-container px-4 py-2 text-sm font-bold text-white"><span className="material-symbols-outlined text-base">add</span>Add Service</button></div><div className="space-y-3">{services.map((service, index) => <div key={service.name} className="flex flex-col gap-4 rounded-2xl bg-surface-container p-5 sm:flex-row sm:items-center"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-secondary/10 text-secondary"><span className="material-symbols-outlined">{['restaurant', 'luggage', 'celebration', 'spa'][index % 4]}</span></div><div className="min-w-0 flex-1"><h2 className="font-bold text-on-surface">{service.name}</h2><p className="mt-1 text-sm text-on-surface-variant">{service.description}</p></div><span className="text-sm text-on-surface-variant">{service.profiles} profiles</span><button onClick={() => toggle(service.name)} className={'relative h-6 w-11 rounded-full transition-colors ' + (service.active ? 'bg-secondary' : 'bg-surface-container-highest')}><span className={'absolute top-1 h-4 w-4 rounded-full bg-white transition-transform ' + (service.active ? 'translate-x-6' : 'translate-x-1')} /></button><button className="text-on-surface-variant hover:text-secondary"><span className="material-symbols-outlined">more_horiz</span></button></div>)}</div></div></AdminShell>; }
